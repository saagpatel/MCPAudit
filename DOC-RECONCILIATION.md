# DOC-RECONCILIATION.md

This file was generated by the `/doc-truth-up` documentation-reconciliation pass.
It treats the code as read-only ground truth and edits only documentation so it
reflects the repo's actual current state — it changed no code and ran no builds
or tests. Its purpose is to give a reviewer a compact, auditable sign-off on what
was found and what was changed.

---

## Per-Claim Findings

### 1. What it is

**Status: consistent**
Evidence basis: verified-by-reading-code

`README.md`, `CLAUDE.md`, and `AGENTS.md` all describe the tool as a local-first
Python CLI that scans MCP server configurations, classifies risk, detects
prompt-injection patterns, and reports without leaking secrets or mutating configs.
This matches the entry point at `src/mcp_audit/cli.py:43` (`mcp-audit = "mcp_audit.cli:main"`)
and the `pyproject.toml` project description. No changes made.

---

### 2. Current state

**Status: drifted (fixed in CLAUDE.md and AGENTS.md)**
Evidence basis: verified-by-reading-code

**CLAUDE.md** (`portfolio-context` section):
- `CLAUDE.md:50` — "**Stable 1.8.0 maintenance**": `pyproject.toml:3` shows `version = "1.12.0"`;
  git log shows releases 1.9.0 through 1.12.0 after commit `3fb6e52` (the last version correction).
  → fixed: "1.8.0" → "1.12.0"
- `CLAUDE.md:51` — capability description omitted three features added since 1.8.0:
  launch-artifact integrity detection (`--integrity-check`, MCP024, added 1.10.0 in `src/mcp_audit/integrity.py`);
  registry package verification (`--verify-artifacts`, MCP025, added 1.11.0 in `src/mcp_audit/pkgverify.py`);
  byte-level artifact verification (`--download-artifacts`, MCP026, added 1.12.0 in `src/mcp_audit/pkgverify.py`).
  → fixed: added the three missing capabilities to the description.

**AGENTS.md** (`portfolio-context` section):
- `AGENTS.md:64` — Current State listed only a 1.6.0-era feature set, missing: SSRF detection,
  lethal-trifecta detection, shadowing detection, capability-escalation detection, provenance drift
  detection, launch-artifact integrity detection, registry package verification, byte-level artifact
  verification, HTML output, and config health diagnostics — all of which exist in the codebase at
  `src/mcp_audit/ssrf.py`, `src/mcp_audit/trifecta.py`, `src/mcp_audit/shadowing.py`,
  `src/mcp_audit/escalation.py`, `src/mcp_audit/provenance.py`, `src/mcp_audit/integrity.py`,
  `src/mcp_audit/pkgverify.py`, `src/mcp_audit/htmlreport.py`, and `src/mcp_audit/cli.py`.
  → fixed: rewrote the single-sentence capability summary to enumerate current features.

---

### 3. Stack

**Status: consistent**
Evidence basis: verified-by-reading-code

`pyproject.toml` lists: `click>=8.0`, `rich>=15.0.0`, `anyio>=4.0`, `json5>=0.10`,
`mcp>=1.27.0`, `pydantic>=2.0`, `pyyaml>=6.0`. Optional extras: `watchfiles>=1.1.1` (watch),
`anthropic>=0.40` (llm). Dev deps: `mypy`, `pytest>=9.0.2`, `pytest-anyio`, `ruff`.
All three doc files describe this stack consistently. No changes made.

---

### 4. How to run

**Status: drifted (fixed in CLAUDE.md); AGENTS.md verification command drifted (fixed)**
Evidence basis: verified-by-reading-code

**CLAUDE.md How To Run** (`portfolio-context` section):
- Eleven scan flags present in `src/mcp_audit/cli.py:167–222` were absent from the section:
  `--ssrf-check`, `--ssrf-allowlist`, `--trifecta-check`, `--shadow-check`,
  `--escalation-check`, `--provenance-check`, `--integrity-check`, `--verify-artifacts`
  (both `pin` and `scan` variants), `--download-artifacts` (both variants), and `--html`.
  → fixed: added all missing commands in the correct CLI groupings; updated the export
  comment from "JSON or SARIF 2.1.0" to "JSON, SARIF 2.1.0, or a self-contained HTML report".

**AGENTS.md Verification section**:
- `AGENTS.md:43` — stated canonical verifier as `uv run mypy src`; actual
  `.codex/verify.commands` file (the authoritative source AGENTS.md points to) contains
  `uv run mypy .`.
  → fixed: updated to `uv run mypy .`; removed the stale follow-up note about
  test-only typing debt blocking the whole-repo gate (the gate is already `mypy .`).

**README.md How To Run** — already complete and consistent with cli.py. No changes.

---

### 5. Known risks

**Status: consistent**
Evidence basis: verified-by-reading-code

`README.md`, `CLAUDE.md`, and `AGENTS.md` all state: no credential value logging/transmit,
no MCP config mutation, LLM analysis opt-in only, subprocess timeouts required,
no hardcoded config paths. These match the coded constraints visible in
`src/mcp_audit/cli.py` (env key-name only reporting) and `src/mcp_audit/redaction.py`.
No changes made.

---

### 6. Next move

**Status: unverifiable**
Evidence basis: unverifiable-because-forward-looking

`README.md` and the portfolio-context sections describe "maintain stable scanner and
output contracts" as the next move. The concrete roadmap items in `docs/ROADMAP-NEXT.md`
are descriptive of work completed through 1.6.0; post-1.6.0 planning documents
(if any) are not present in a structured way. Marking unverifiable — the code shows
what is built, but what comes next requires operator intent, not code reading.
No changes made.

---

## Contradictions for Manual Review

The following files outside the editable set contain stale information a human
should correct:

| File | Location | Issue | Suggested fix |
|------|----------|-------|---------------|
| `HANDOFF.md` | Line 5 | "Latest on PyPI: `1.11.0`" — `pyproject.toml:3` shows 1.12.0 is current | Change `1.11.0` → `1.12.0` |
| `HANDOFF.md` | Line 8 | Refers to "1.9.0" under "Completed this session" header, mislabelled (content is 1.9.0/1.10.0/1.11.0 work); 1.12.0 work (MCP026 byte-level verification) is unmentioned | Add a "1.12.0" entry for byte-level artifact verification |
| `docs/ROADMAP-NEXT.md` | Line 1 | Opening paragraph frames the document as the roadmap for 1.6.0 ("MCPAudit `1.6.0` adds…") but the project is now at 1.12.0; the remaining sections describe historical completions. A new "current roadmap" section for 1.13.0+ would reground readers. | Add a brief current-state header noting this doc covers through 1.6.0 and is now historical context, or add a current roadmap section |

---

## Footer

Generated by `/doc-truth-up` documentation-reconciliation pass.

**Date/time:** 2026-06-02 20:47:46 PDT
**Branch:** `docs/truth-up-2026-06-02`
**HEAD sha reconciled against:** `9fa3d4d194ad23edcae99c6134d9917933cab6e3`
