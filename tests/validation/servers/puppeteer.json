{
	"server_name": "server-puppeteer",
	"description": "@modelcontextprotocol/server-puppeteer — browser automation via Puppeteer",
	"tools": [
		{
			"name": "puppeteer_navigate",
			"description": "Navigate to a URL.",
			"input_schema": {
				"type": "object",
				"properties": {
					"url": { "type": "string" }
				},
				"required": ["url"]
			}
		},
		{
			"name": "puppeteer_screenshot",
			"description": "Take a screenshot of the current page or a specific element.",
			"input_schema": {
				"type": "object",
				"properties": {
					"name": { "type": "string" },
					"selector": { "type": "string" },
					"width": { "type": "number" },
					"height": { "type": "number" }
				},
				"required": ["name"]
			}
		},
		{
			"name": "puppeteer_click",
			"description": "Click an element on the page.",
			"input_schema": {
				"type": "object",
				"properties": {
					"selector": { "type": "string" }
				},
				"required": ["selector"]
			}
		},
		{
			"name": "puppeteer_fill",
			"description": "Fill out an input field.",
			"input_schema": {
				"type": "object",
				"properties": {
					"selector": { "type": "string" },
					"value": { "type": "string" }
				},
				"required": ["selector", "value"]
			}
		},
		{
			"name": "puppeteer_select",
			"description": "Select an element on the page using Select element.",
			"input_schema": {
				"type": "object",
				"properties": {
					"selector": { "type": "string" },
					"value": { "type": "string" }
				},
				"required": ["selector", "value"]
			}
		},
		{
			"name": "puppeteer_hover",
			"description": "Hover an element on the page.",
			"input_schema": {
				"type": "object",
				"properties": {
					"selector": { "type": "string" }
				},
				"required": ["selector"]
			}
		},
		{
			"name": "puppeteer_evaluate",
			"description": "Execute JavaScript in the browser console.",
			"input_schema": {
				"type": "object",
				"properties": {
					"script": { "type": "string" }
				},
				"required": ["script"]
			}
		}
	],
	"expected_findings": [
		{
			"tool": "puppeteer_navigate",
			"categories": ["network"],
			"min_confidence": "high"
		},
		{
			"tool": "puppeteer_evaluate",
			"categories": ["shell_execution"],
			"min_confidence": "medium"
		},
		{
			"tool": "puppeteer_screenshot",
			"categories": ["network"],
			"min_confidence": "low"
		},
		{
			"tool": "puppeteer_click",
			"categories": ["network"],
			"min_confidence": "low"
		},
		{
			"tool": "puppeteer_fill",
			"categories": ["network"],
			"min_confidence": "low"
		}
	]
}
